# v2026.02.03 — локальный кандидат r3

Исправлены состояния LCD при остановке Gateway и восстановление безопасных прав корня CF перед проверкой структуры при штатном старте. Заводской сценарий Moxa мог выставлять 0777; теперь старт приложения приводит права к 0755 под блокировкой установщика. RNG-файлы не удаляются. Остановка RNG имеет ограниченное ожидание; тайм-аут не приводит к принудительному завершению его транзакции.

Проверки LCD normal/UBSan: по 247, ошибок 0. Тест закрытия Web/RNG normal/UBSan проверяет пустое состояние, ограниченное ожидание, сохранение живого RNG при тайм-ауте и ошибку waitpid. Проверки оркестратора установщика выполнялись до последней правки порядка входа; окончательный вход r3 дополнительно проверен на приборе.

Moxa #3, 10.0.2.17, 2026-09-12:
- Реальный service start после выставления корня CF в 0777 восстановил 0755; Gateway/RNG/Web запустились, RNG ready schema 3, generation 13.
- После контрольного отключения/включения питания приложение запустилось автоматически. На первом чтении uptime 0 минут процессов ещё не было; при uptime 3 минуты они уже работали. Точное время загрузки не измерялось.
- HTTP GET /: 200, 64551 байт, 0,098102 с без прокси. Это один запрос, не нагрузочный тест.
- RTC и ОС: 07:45:50 и 07:45:49 UTC. Настройка UTC на этом приборе выполнена отдельно и не является изменением пакета.
- Пользователь сфотографировал штатный экран Gateway stopped. Отдельная аппаратная имитация Shutdown failed/F1 не проводилась; этот путь проверен локальными тестами.

Установочный кандидат r3 SHA256: `70db31c9e8f37f60f6040d609e5ca3d72f6e726492d098c47dc59f4755258527`.

Это локальная фиксация, не публикация релиза. Web UI остановки не изменён. Проверки питания внутри NV-транзакции и ресурса CF этим опытом не заменяются.

[Экран и навигация](v2026.02.03-lcd.ru.md)
