# Vendor artifact staging

Place externally acquired SDK and toolchain archives here for local inspection.
The directory is ignored except for this file. Do not commit vendor binaries,
installed toolchains, or large archives without explicit approval. Record the
source URL, publisher checksum, local checksum, version, and inspection result
under `docs/forensics/`.
