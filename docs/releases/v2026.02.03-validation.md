# v2026.02.03 — local r3 candidate

The fix adds LCD shutdown results and repairs CF root permissions before application-start layout validation, under the installer lock. Vendor startup could set mode 0777; application startup restores 0755. RNG files are not deleted. RNG shutdown waits for a bounded interval without forcibly terminating its transaction on timeout.

LCD normal/UBSan: 247 checks each, zero failures. The Web/RNG close test covers empty state, bounded waiting, a surviving RNG on timeout, and waitpid failure in both modes. Installer orchestration tests preceded the final entry-order fix; the actual r3 entry was additionally checked on hardware.

Moxa #3, 10.0.2.17, 2026-09-12:
- Actual service start restored CF root 0777 to 0755; Gateway/RNG/Web started, RNG ready schema 3, generation 13.
- Application autostart passed after a control power cycle. Processes were absent at the first uptime-0-minute sample and present at uptime 3 minutes. Exact boot duration was not measured.
- HTTP GET /: 200, 64551 bytes, 0.098102 seconds without a proxy. One request, not a load test.
- RTC/OS: 07:45:50 / 07:45:49 UTC. UTC configuration was a separate device operation, not a package change.
- The user photographed Gateway stopped. Shutdown failed/F1 was covered by local tests, not a separately induced hardware failure.

Installer r3 SHA256: `70db31c9e8f37f60f6040d609e5ca3d72f6e726492d098c47dc59f4755258527`.

This is a local commit, not a published release. The Web shutdown UI is unchanged. This test does not qualify power loss inside NV transactions or CF endurance.

[Screen and navigation](v2026.02.03-lcd.md)
