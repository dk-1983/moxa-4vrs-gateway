# Moxa #2: отключение и возврат LAN1, 2026-09-11

Кандидат web-ipc-v1, SHA256 установочного пакета 92e35241d9ee8d3fa8c9420803b55f0c793a8c1755ecf9f5e5133eb9babd0100. Оператору предложено отключить LAN1 на30 секунд и вернуть без выключения питания; возврат подтверждён сообщением «Включил». Точная длительность физического разрыва не измерена.

Наблюдение через COM3,115200,8N1 с интервалом15 секунд. В sample4 (elapsed60,04с) у eth0 исчез RUNNING, Web-процесс отсутствовал. В sample5 (elapsed75,04с) RUNNING вернулся, Web запущен с новым PID1526 вместо967. Во всех этих снимках Gateway PID945 сохранился, RNG ready, generation42. Следующий sample6 подтвердил стабильное состояние.

После возврата HTTP200 за19,181мс. TCP-соединения внешнего клиента192.0.2.4 к портам502/503 восстановились с новыми клиентскими портами. Ручной перезапуск, recovery или изменение конфигурации не выполнялись. По поколениям не наблюдался новый startup commit RNG; фактические блочные/NAND-записи этим не измерялись.

Результат: один физический обрыв/возврат LAN1 успешно восстановил доступ HTTP и клиентские TCP; основной процесс Gateway оставался жив. Непрерывность/успех UART/Modbus-транзакций, авторизация, HTTPS, DHCP/DNS и смена адреса этим тестом не проверены. Утверждение о точном времени восстановления из15-секундных снимков не делается.

Первичные данные: serial.jsonl и after.txt рядом с отчётом. Секреты не экспортировались.
