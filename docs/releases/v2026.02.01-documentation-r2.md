# v2026.02.01 documentation correction — 12 September 2026

The original publication retained an incomplete LCD tree, insufficient Web instructions and old installation commands. These errors are corrected in main and the `docs-r2` archives.

- RU/EN guides include complete LCD/Web trees, actions on all seven Web pages, backlight, protocol, access codes and certificates.
- Each guide contains 27 unique illustrations, image left and menu path right. Relative links and local RU/EN gallery rendering were checked.
- Installation now covers Ubuntu 24.04 dependencies, download/checksums, CF preparation, separate initial RNG activation, installer execution and terminal-result verification. Lab-specific device and UUID requirements were removed.
- `tools/cf-package.py` now pins the installer already present in the published wizard. The other wizard scripts match the original release.
- Checks covered real unzip, worker executable mode, both manifests, all 11 package files, rejection of damaged packages/kits, five XScale ELF ABIs and RU/EN command syntax. Checks ran in Debian 12 and Ubuntu 24.04 containers, with Ubuntu dependencies installed according to the instructions.

The installer archive remains byte-identical: SHA256 `624f807edb127977e09dde918460653b32b2b6f3590869080a6c144e6478844c`. Files under `src/`, wizard executables and embedded installer are unchanged. New archive contents and checksums are verified separately. The original release tag is retained; corrected documentation is in main and the `docs-r2` archives.

This documentation audit is not another physical device installation. Existing hardware evidence and limits remain unchanged: [release matrix](../release-final.md). Power loss within NV writes, NAND endurance and load on all eight physical ports are not qualified by this audit.
