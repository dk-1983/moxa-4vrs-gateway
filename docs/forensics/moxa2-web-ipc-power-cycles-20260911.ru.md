# Установка web-ipc-v1 на Moxa #2

Пакет SHA256 92e35241d9ee8d3fa8c9420803b55f0c793a8c1755ecf9f5e5133eb9babd0100 проверен. Один запуск установщика, итог result0 verify-installed. Первое чтение result100 было промежуточным, установка не повторялась. Четыре установленных ELF сверены с архивом; gateway.conf совпала с резервной копией на CF.

Gateway PID4309, RNG4311, Web4327. RNG ready schema3 generation32 accounted29. IPC каталог0711, lock0600, socket0666, действующий путь присутствует в /proc/net/unix. HTTP200 за0,015059с. Клиентские соединения P1/P2 вернулись; Modbus-транзакции и авторизация отдельно не проверены.

Новая серия физических power-cycle пока0/20. Старый цикл2 остаётся FAIL. Сохранённый старый IPC-объект не изменялся; recovery RNG не вызывался.

Новая серия, цикл1: автономный запуск Gateway/RNG/Web успешен, generation33, HTTP200 за102,5мс, IPC945.sock активен, P1/P2 TCP восстановлены. Снимок при uptime55,51с; точное время от подачи питания не измерено. Обмен Modbus по транзакциям отдельно не проверен.

Цикл2: автономный Gateway/RNG/Web успешен при повторном PID945 и имени IPC945.sock; generation34, HTTP200 за143,7мс, P1/P2 TCP восстановлены. Ручных интервенций не было. Снимок при uptime54,92с; точная длительность запуска и Modbus-транзакции не измерены.

Цикл3: автономный Gateway/RNG/Web успешен, generation35, HTTP200 за97,1мс, IPC945.sock активен, P1/P2 TCP восстановлены. Без ручных интервенций; снимок при uptime53с. Modbus-транзакции отдельно не измерены.

Цикл4: две ранние SSH-попытки недоступны. При uptime51,74с RNG ready36, Web и клиентские TCP ещё отсутствовали в снимке; последующий GET200 за101мс. При uptime75,63с Web979, IPC945.sock активен, оба TCP восстановлены. Восстановление автономное, без вмешательств. Точное время подачи питания и Modbus-транзакции не измерены.

Цикл5: автономный Gateway/RNG/Web успешен, generation37, HTTP200 за95,6мс, IPC945.sock активен, оба TCP восстановлены. Снимок при uptime53,98с. Оператор сообщил о немедленном начале загрузки. Modbus-транзакции отдельно не измерены.

Цикл6: автономный Gateway/RNG/Web успешен, generation38, HTTP200 за98,6мс, IPC945.sock активен, оба TCP восстановлены. Снимок при uptime38,15с; точное время подачи питания и Modbus-транзакции не измерены.

Цикл7: автономный Gateway/RNG/Web успешен, generation39, HTTP200 за98,9мс, IPC945.sock активен, оба TCP восстановлены. Снимок при uptime57,04с; Modbus-транзакции отдельно не измерены.

Цикл8: автономный Gateway/RNG/Web успешен, generation40, HTTP200 за105,8мс, IPC945.sock активен, оба TCP восстановлены. Снимок при uptime50,10с; Modbus-транзакции отдельно не измерены.

Цикл9: автономный Gateway/RNG/Web успешен, generation41, HTTP200 за97,3мс, IPC945.sock активен. При uptime54,31с TCP клиента отсутствовали; оба восстановились к снимку uptime82,30с. Без ручного вмешательства; Modbus-транзакции отдельно не измерены.

## Серия завершена: 10 физических циклов

По явному решению пользователя объём сокращён с20 до10. Выполнены10/10 автономных восстановлений Gateway/RNG/Web после отключения питания при завершённом commit. Поколения33–42, HTTP200 во всех циклах, диапазон отдельного GET после запуска95,563–143,709мс. Путь IPC с PID945 повторялся и восстанавливался без ручной очистки. Вмешательств и recovery между циклами не было. Старый цикл2 прежнего кандидата остаётся FAIL.

Соединения P1/P2 восстановились во всех10 циклах; в циклах4 и9 потребовалось дополнительное наблюдение. Успех всех Modbus-транзакций не измерен. Время HTTP не является временем загрузки ОС. Точное время подачи питания инструментально не записывалось, поэтому времена запуска по монотонным часам стенда не заявляются; сохранены uptime снимков и ранние неуспешные попытки связи.

Это завершённая сокращённая серия проверки автономного запуска, а не полная квалификация всех пунктов аппаратного плана. Авторизация/HTTPS, счётчики Modbus, питание внутри NV-транзакции, неисправности CF и ресурс NAND этой серией не доказаны. Следующий этап: обрыв/возврат LAN на работающем приборе с наблюдением HTTP, RNG и восстановления клиентских соединений; затем выделенная запасная CF для испытаний записи.
